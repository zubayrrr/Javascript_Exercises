// Create a function that takes length and width and finds the perimeter of a rectangle. P=2(l+w)

const perimeterOfRectangle = (len, wid) => {
  return 2 * (len + wid);
};

console.log(perimeterOfRectangle(2, 5));
